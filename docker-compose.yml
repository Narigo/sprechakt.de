volumes:
  pnpm-cache:

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - ./:/app
      - pnpm-cache:/root/.pnpm-store
    ports:
      - "3000:3000"
